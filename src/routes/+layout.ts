// Tauri is a thin native window around a static SPA.
// SSR off (no server); prerender off (we run client-only at runtime).
export const ssr = false;
export const prerender = false;
export const trailingSlash = 'ignore';
